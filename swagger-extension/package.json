{
  "name": "swagger-extension",
  "version": "1.0.0",
  "description": "",
  "main": "background.js",
  "scripts": {
    "migrate": "mkdir -p dist && cp -r images popup.html manifest.json *.css  dist/",
    "build": "esbuild src/entry/*.ts --bundle --outdir=dist --minify --sourcemap=both --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "0.17.18"
  }
}
